services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    command: ["streamlit", "run", "resume_calibrator.py"]
    deploy:
      resources:
        limits:
          memory: 4g
        reservations:
          memory: 2g

version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resume_calibrator_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - ./model_new:/app/model_new
    environment:
      - MODEL_PATH=/app/model_new
